package com.bmh.utils;

import java.text.DecimalFormat;
import java.text.NumberFormat;
import java.text.SimpleDateFormat;
import java.util.Date;
import java.util.Locale;


public class ConverterChiffresEnLettres {
/*
	public static void main(String[] args) {
		String resultatAR;
		String resultatFR;
		String nombre;
		double budget = 454127879977.57;
		if (Double.toString(budget).contains(",")) {
			nombre = Double.toString(budget).replace(',', '.');
		}else{
			nombre = Double.toString(budget);
		}
		resultatAR = Convert.AR(nombre);
		resultatFR = Convert.FR(nombre);
		System.out.println("R1 : " + String.format("%.2f",budget));
		System.out.println("R2 : " + resultatAR);
		System.out.println("R3 : " + resultatFR);


		double soldeBancaire = 45412787;
        NumberFormat numberFormat = NumberFormat.getInstance(java.util.Locale.FRENCH);


         //Afficher avec le regles du pays

        System.out.println(numberFormat.format(soldeBancaire));
        //resultat: 56 789,12

        String pattern = "###,###.00";
        DecimalFormat decimalFormat = new DecimalFormat(pattern);

        String format = decimalFormat.format(soldeBancaire);
        System.out.println(format);

        // ************************************************

        Locale locale = new Locale("ar");
        SimpleDateFormat sdf = new SimpleDateFormat("EEEE dd MMMM yyyy",locale);
        Date d = new Date();
        String format1 = sdf.format(d);
        System.out.print("Result: " + "\u064a\u0648\u0645 "+format1);
	}
	*/
}
